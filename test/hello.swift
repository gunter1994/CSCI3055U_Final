print ("Hello World")

var mutable = 6
print ("vars can change, mutable: " + String(mutable))
mutable = 8
print ("mutable: " + String(mutable))
let constant = "something"
print ("using let makes a constant, constant: " + constant) 
//constant = "something else" makes an error
